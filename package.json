{
  "name": "imfinewithwhatever",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "install": "cd ui && npm i && cd ../server && npm i",
    "test": "echo \"Error: no test specified\" && exit 1",
    "debug-ui": "cd ui && npm run start",
    "debug-development-server": "set NODE_ENV=development && cd server && npm run debug",
    "debug-production-server": "set NODE_ENV=production && cd server && npm run start"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.3.5",
    "koa": "^2.14.1"
  }
}